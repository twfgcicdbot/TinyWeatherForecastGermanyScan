tasks:
- init: sudo apt install -y apktool && sudo apt install -y dexdump && sudo apt install -y ripgrep && python -m pip install --upgrade pip && pip install -r requirements.txt
vscode:
  extensions:
    - ms-python.python
    - ms-toolsai.jupyter
    - eamodio.gitlens